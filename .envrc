# direnv development environment for bible-reader.
# Create the venv first with: python -m venv .venv

if [ -d .venv ]; then
  source .venv/bin/activate
else
  echo "bible-reader: .venv not found; run: python -m venv .venv"
fi

export PYTHONPATH="$PWD/src${PYTHONPATH:+:$PYTHONPATH}"
